# API example using Flask

To start the project run the following commands:

-create a virtual environment by running py -m venv venv
-install the requirements by running pip install -r requirements.txt
